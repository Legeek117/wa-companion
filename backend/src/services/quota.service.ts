// import { getSupabaseClient } from '../config/database';
// import { QuotaExceededError } from '../utils/errors';

// TODO: Implement quota service
export const quotaService = {
  // checkViewOnceQuota: async (userId: string) => {},
  // checkDeletedMessagesQuota: async (userId: string) => {},
  // checkScheduledStatusQuota: async (userId: string) => {},
  // incrementViewOnce: async (userId: string) => {},
  // incrementDeletedMessages: async (userId: string) => {},
  // incrementScheduledStatus: async (userId: string) => {},
  // resetMonthlyQuotas: async () => {},
};

