import { Router } from 'express';
// import { viewOnceController } from '../controllers/viewOnce.controller';
// import { authenticateToken } from '../middleware/auth.middleware';

const router = Router();

// TODO: Implement routes
// router.get('/', authenticateToken, viewOnceController.getCaptures);
// router.get('/:id', authenticateToken, viewOnceController.getCapture);
// router.get('/:id/download', authenticateToken, viewOnceController.downloadCapture);

export default router;

