import { Router } from 'express';
// import { scheduledStatusController } from '../controllers/scheduledStatus.controller';
// import { authenticateToken } from '../middleware/auth.middleware';

const router = Router();

// TODO: Implement routes
// router.get('/', authenticateToken, scheduledStatusController.getScheduled);
// router.post('/', authenticateToken, scheduledStatusController.create);
// router.put('/:id', authenticateToken, scheduledStatusController.update);
// router.delete('/:id', authenticateToken, scheduledStatusController.delete);

export default router;

