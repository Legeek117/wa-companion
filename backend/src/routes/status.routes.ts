import { Router } from 'express';
// import { statusController } from '../controllers/status.controller';
// import { authenticateToken } from '../middleware/auth.middleware';

const router = Router();

// TODO: Implement routes
// router.get('/', authenticateToken, statusController.getStatuses);
// router.post('/like', authenticateToken, statusController.likeStatus);
// router.get('/config', authenticateToken, statusController.getConfig);
// router.put('/config', authenticateToken, statusController.updateConfig);

export default router;

