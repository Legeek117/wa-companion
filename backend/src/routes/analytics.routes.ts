import { Router } from 'express';
// import { analyticsController } from '../controllers/analytics.controller';
// import { authenticateToken, requirePremium } from '../middleware/auth.middleware';

const router = Router();

// TODO: Implement routes (Premium only)
// router.get('/overview', authenticateToken, requirePremium, analyticsController.getOverview);
// router.get('/status', authenticateToken, requirePremium, analyticsController.getStatusStats);
// router.get('/view-once', authenticateToken, requirePremium, analyticsController.getViewOnceStats);
// router.get('/deleted-messages', authenticateToken, requirePremium, analyticsController.getDeletedMessagesStats);

export default router;

