import { Router } from 'express';
// import { autoresponderController } from '../controllers/autoresponder.controller';
// import { authenticateToken } from '../middleware/auth.middleware';

const router = Router();

// TODO: Implement routes
// router.get('/config', authenticateToken, autoresponderController.getConfig);
// router.put('/config', authenticateToken, autoresponderController.updateConfig);
// router.get('/contacts', authenticateToken, autoresponderController.getContacts);
// router.put('/contacts/:id', authenticateToken, autoresponderController.updateContact);

export default router;

