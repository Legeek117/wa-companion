import { Router } from 'express';
// import { deletedMessagesController } from '../controllers/deletedMessages.controller';
// import { authenticateToken } from '../middleware/auth.middleware';

const router = Router();

// TODO: Implement routes
// router.get('/', authenticateToken, deletedMessagesController.getMessages);
// router.get('/:id', authenticateToken, deletedMessagesController.getMessage);
// router.get('/export', authenticateToken, deletedMessagesController.exportMessages);

export default router;

